# 壳工程局部忽略（忽略规则收在本目录内，与 Web 侧 .gitignore 互不干扰）
node_modules
www
.DS_Store
*.local
# 原生工程构建产物（android/ios 目录内模板自带的 .gitignore 兜底）
android/.gradle
android/app/build
android/build
android/local.properties
android/.idea
ios/App/build
ios/DerivedData
ios/App/Pods
ios/App/output
