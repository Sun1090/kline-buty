{
  "name": "kline-buty-app-shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Capacitor 壳工程（app 分支专用）。Web 产物来自仓库根的 dist/，本目录不包含业务代码。",
  "scripts": {
    "web:sync": "node scripts/sync-web.mjs",
    "cap:sync": "npm run web:sync && cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
