# 06 · 开发任务清单（2026-08-29）

> 30 个开发任务（测试另计，按 `docs/agents/coding.md` 惯例随任务附带）。
> 排序原则：P0 核心体验对齐 OKX/币安 → P1 交易辅助 → P2 深化与生态。
> 执行约定：一个任务一个提交；五语 i18n 同步；每任务过 `typecheck/lint/test`，涉及图表行为加 E2E 场景。
>
> **状态（2026-08-30）：30 项全部完成。** T2/T11/T13/T23/T24 实施中核查为已有功能无需重做；T29 并入 T10 同一实现。遗留跟进项：09 章全面导读卡化（需先迁移深度内容到专章）。

## P0 核心体验

| # | 任务 | 验收 |
|---|---|---|
| T1 | 周期收盘倒计时：行情信息条显示当前周期剩余时间 | 每秒刷新，切周期重算，五语 |
| T2 | 资金费率显示：信息条增加当前资金费率（fapi + dapi 兜底链） | 有值/无值降级，五语 |
| T3 | MarketList 搜索过滤框 | 即时过滤、清空恢复、与排序共存 |
| T4 | MarketList 自选/全部视图切换 | 自选空态引导，切换保持搜索 |
| T5 | 主题跟随系统：ThemePicker 增加自动档（prefers-color-scheme） | 系统切换实时生效并持久化 |
| T6 | 键盘方向键移动十字光标，Esc 清除 | 不打断画线/文本编辑，移动端不介入 |
| T7 | 时间轴时区切换（UTC/本地），持久化 | 刻度与十字光标信息同步切换 |
| T8 | 副图指标 BBW 布林带宽（复用 calcBOLL） | 数值边界 + 参数面板 + i18n |
| T9 | 主图指标 Supertrend | 算法纯函数 + 渲染 + i18n |
| T10 | 图表首屏骨架屏（shimmer 占位） | 首次加载显示，K 线到达消失 |

## P1 交易辅助与提醒

| # | 任务 | 验收 |
|---|---|---|
| T11 | 价格提醒桌面通知（Notification API + 权限申请） | 触发时系统通知，被拒降级 |
| T12 | 提醒触发声音（WebAudio 短音，可开关） | 默认关，持久化 |
| T13 | 提醒触发历史（最近 20 条，localStorage） | 面板内查看/清空 |
| T14 | QuickOrder 买一/卖一价一键填入 | 点击即填，市价单不可用置灰 |
| T15 | 模拟交易引擎 v1：市价单本地撮合 | 按最新价成交、余额扣减、与仓位面板联动 |
| T16 | 交易流水面板（模拟成交流水） | 列表 + 清空，持久化 |
| T17 | QuickOrder 百分比仓位按钮（25/50/75/100%，基于模拟余额） | 计算数量，余额不足提示 |
| T18 | 画线吸附开关（吸附 K 线 OHLC） | 提交/拖拽锚点均生效，默认关 |
| T19 | 画线 JSON 导出/导入 | 导出含品种/周期校验，导入去重 |
| T20 | 图层批量操作：全部显示/全部隐藏 | 与单行状态联动 |

## P2 深化与生态

| # | 任务 | 验收 |
|---|---|---|
| T21 | 四图布局每格独立周期 | 格内周期选择器，联动其它格不变 |
| T22 | 移动端下拉刷新 K 线 | 顶部下拉触发重新拉取，带加载态 |
| T23 | PWA manifest + 安装图标 | 可安装、图标与主题色正确 |
| T24 | Service Worker 静态壳缓存 | 离线打开显示缓存壳与离线提示 |
| T25 | 文档站文章底部「相关阅读」（同章节 3 篇） | 构建期生成，链接可达 |
| T26 | 文档站阅读进度条 + 返回顶部 | 长文页显示，不遮挡内容 |
| T27 | 图表右键菜单：复制价格/添加提醒/清空画线 | 桌面右键，菜单项五语 |
| T28 | 快捷键帮助浮层分组 + 搜索 | 按功能分组、关键字过滤 |
| T29 | K 线首屏骨架屏（与 T10 合并为同一实现） | — |
| T30 | 面板空态/错误态统一组件 | 加载失败重试按钮，五语 |

## 已知欠账（不占 30 名额）

- TypeScript 7：等 typescript-eslint 支持（当前 peer <6.1.0）
- VitePress 2 正式版：升级后可移除 `.npmrc` legacy-peer-deps 并顺带清掉唯一 audit 链
- app-shell M1–M3：需真机与上架决策，见 `app-shell/docs/plan.md`
