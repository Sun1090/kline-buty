# 知识库视觉增强与质量修复 · 完整任务清单

---

## A6 · 示范改造（最先执行）

**目标**：选1-2篇文章做完整视觉增强，验证VitePress渲染效果，确认样式无误后再批量铺开。

**推荐文件**：
- `docs/knowledge/01-入门基础/02-交易核心概念.md`（术语密集、有表格、有SVG、有风险提示）
- `docs/knowledge/03-期货篇/02-保证金杠杆与强平.md`（公式多、数字例子多、风险内容多）

**改造内容**：
1. 核心术语首次出现加 `**<mark>术语</mark>**`
2. 正文中的风险提示改为 `::: danger`，注意事项改为 `::: warning`，结论口诀改为 `::: tip`，术语定义改为 `::: info`
3. 推导过程/扩展内容加 `<details><summary>` 折叠
4. 表格中关键数字加 `<mark>`
5. 无语言标识的代码块加 ` ```text `
6. 添加 frontmatter（title + description）

**验证方法**：本地 `npm run docs:dev` 启动VitePress，检查：
- `<mark>` 是否蓝底高亮
- 四色容器是否正确渲染（红/橙/蓝/青）
- `<details>` 折叠是否正常展开/收起
- frontmatter 是否出现在页面标题和浏览器标签
- 表格、代码块是否正常

---

## B2 · 死链接排查

**目标**：逐一验证254处交叉引用的目标文件是否存在。

**操作步骤**：
1. 提取所有交叉引用：`grep -ro '\[.*\](\.\./.*\.md)' docs/knowledge/ | sort | uniq`
2. 对每个引用路径，验证目标文件是否存在（注意相对路径解析）
3. 重点排查可疑引用：
   - `09-市场与品种专题篇/08-宏观经济与市场.md`（需确认08号是否存在）
   - `07-交易系统篇/03-交易心理.md`（需确认该文件路径）
   - `15-量化实战篇/04-实盘自动化.md`（需确认该文件路径）
4. 输出死链接清单：文件路径 + 行号 + 引用路径 + 缺失目标
5. 修复：更新路径或创建缺失文件

---

## C2 · 风险提示格式统一

**目标**：全库435处风险提示/免责声明统一为标准格式。

**现状问题**：
- 有的用 `> **免责声明**：...`（引用块+加粗）
- 有的用 `> ⚠️ 风险提示` + 正文（引用块）
- 有的用 `::: danger ⚠️ ...`（容器）
- 格式混乱，视觉不统一

**统一规则**：

**文章开头免责声明**（每篇都有，统一用引用块）：
```markdown
> **免责声明**：本站全部内容仅用于学习与研究，不构成任何投资建议。市场有风险，投资需谨慎。
```

**正文中的致命风险**（爆仓、穿仓、归零等）：
```markdown
::: danger ⚠️ 标题
内容
:::
```

**正文中的注意事项**（容易踩的坑、常见误区）：
```markdown
::: warning ⚠️ 标题
内容
:::
```

**正文中的结论/口诀**：
```markdown
::: tip 💡 标题
内容
:::
```

**正文中的术语定义/补充资料**：
```markdown
::: info 📖 标题
内容
:::
```

**操作步骤**：
1. 全文搜索 `免责声明`、`风险提示`、`⚠️`、`危险`、`注意` 等关键词
2. 按上述规则分类替换
3. 注意：`::: danger/tip` 容器内不要再加引用块 `>`，会嵌套出错

---

## C4 · frontmatter 补充

**目标**：120+篇文章添加 title + description frontmatter，修复SEO和社交分享卡片。

**现状**：全库0篇文章有frontmatter，VitePress默认用第一个h1做标题，无description。

**统一格式**：
```markdown
---
title: 02 · 交易核心概念
description: 交易术语字典——杠杆、保证金、爆仓、盘口、滑点、市价单、限价单，每个概念通俗解释+一句话例子+计算示例
---

# 02 · 交易核心概念
```

**规则**：
- `title`：与h1标题一致，格式为 `编号 · 篇名`
- `description`：一句话概括本篇内容，包含2-3个核心关键词，不超过120字
- 每篇README不需要frontmatter（VitePress侧边栏用config配置）

**操作步骤**：
1. 遍历 `docs/knowledge/` 下所有 `*.md` 文件（排除README）
2. 读取每篇h1标题和开头段落，生成title和description
3. 在文件开头插入frontmatter

---

## A1 · 术语高亮

**目标**：核心术语首次出现添加 `**<mark>术语</mark>**`。

**标记规则**：
- **仅标记首次出现**，同一篇内重复出现不加mark
- **标记范围**：

| 类别 | 术语示例 |
|---|---|
| 风险关键词 | 爆仓、穿仓、强平、归零、追加保证金、负余额 |
| 核心机制 | 保证金、杠杆、逐日盯市、资金费率、滑点、点差 |
| 术语首次定义 | 买入价/卖出价、ask/bid、一档/五档、市价单/限价单 |
| 关键结论 | 利差是汇率的第一驱动力、收益和损失被同倍放大 |

**写法**：
```markdown
保证金是开杠杆仓位时抵押给交易所的**<mark>押金</mark>**
10倍杠杆做多，价格下跌10%，保证金归零——**<mark>爆仓</mark>**
```

**操作步骤**：
1. 每篇文章从头阅读，识别核心术语首次出现位置
2. 将 `**术语**` 改为 `**<mark>术语</mark>**`
3. 同一篇内同一术语只标记第一次

---

## A2 · 四色容器补充

**目标**：识别正文中的风险提示、结论、注意事项，补充 `::: danger/warning/tip/info` 容器。

**识别规则**：

| 容器 | 识别信号 |
|---|---|
| `::: danger` | "爆仓""归零""穿仓""全部亏损""骗局"等致命风险 |
| `::: warning` | "注意""需要注意""容易""常见误区""不要"等避坑提醒 |
| `::: tip` | "一句话""记住""核心""关键""公式"等结论口诀 |
| `::: info` | "即""也就是""指的是""补充"等术语定义/扩展说明 |

**操作步骤**：
1. 全文搜索上述信号词
2. 判断上下文是否适合提升为容器（不是所有加粗都需要容器，只在段落级结论/风险时使用）
3. 改写为容器格式，加emoji标题

---

## B1 · 20章编号修复

**目标**：修复20-经典书单篇03→05跳号问题。

**操作步骤**：
1. 重命名文件：
   - `05-量化与交易心理.md` → `04-量化与交易心理.md`
   - `06-如何精读一本书.md` → `05-如何精读一本书.md`
2. 更新20-经典书单篇/README.md中所有引用：
   - `[05-量化与交易心理.md]` → `[04-量化与交易心理.md]`
   - `[06-如何精读一本书.md]` → `[05-如何精读一本书.md]`
   - 五层书单表中第4层/第5层对应编号
3. 全库搜索引用这两个文件的所有交叉引用，逐一更新
4. 更新根README.md中20-经典书单篇的目录表

---

## C1 · 术语统一

**目标**：制定"爆仓"与"强平"混用规则，全库统一。

**现状**：409处"爆仓" vs 212处"强平"，混用无规则。

**统一规则**：
- **首次出现**：写全 `爆仓/强平（Liquidation）`
- **期货篇**：正文用"强平"（期货行业标准术语），口语场景可用"爆仓"
- **加密合约篇**：正文用"爆仓"（加密圈通用说法），技术描述用"强平"
- **其他篇章**：按语境选择，风险提示中优先用"爆仓"（更直白）
- **标题/小标题**：统一用"强平（爆仓）"

**操作步骤**：
1. 制定统一规则文档
2. 逐篇检查，按规则调整
3. 同理处理其他混用术语（如有发现）

---

## C3 · 目录统一

**目标**：决定63篇手写 `## 目录` 保留还是移除，全库统一。

**现状**：63篇有手写目录（锚点链接列表），60+篇没有。VitePress自动生成右侧大纲导航。

**方案A：全部移除**
- 优点：减少维护成本，VitePress大纲导航已覆盖
- 缺点：移动端无大纲导航时缺少目录

**方案B：全部保留**
- 优点：移动端友好，长文快速跳转
- 缺点：维护成本高，新增章节需手动更新

**方案C：仅长文保留（>200行）**
- 优点：折中
- 缺点：标准不清晰

**建议**：方案A，全部移除。VitePress的outline配置已提供目录导航。如需移动端支持，可在config中配置 `outline: { level: [2, 3], label: '目录' }`。

**操作步骤**：
1. 确认VitePress config中outline配置是否开启
2. 如确认移除，批量删除63篇中的 `## 目录` 及其下锚点列表
3. 如确认保留，为其余60+篇补充手写目录

---

## A3 · 可折叠内容

**目标**：推导过程、扩展阅读添加 `<details>` 折叠，降低首屏阅读压力。

**适合折叠的内容**：
- 推导过程、计算步骤（如保证金计算、盈亏推演）
- 历史案例复盘（如1997亚洲金融危机、2008次贷）
- 扩展阅读、"如果你想知道更多"类补充
- 公式证明

**写法**：
```markdown
<details>
<summary>📖 点击展开：保证金计算完整过程</summary>

1. 螺纹钢价格：3500 元/吨，一手 10 吨
2. 合约价值 = 3500 × 10 = 35000 元
3. 保证金比例 10% → 开仓保证金 = 3500 元

</details>
```

---

## A4 · 表格关键单元格高亮

**目标**：表格中关键数字和风险关键词添加 `<mark>`。

**标记范围**：
- 杠杆倍数表中的"100% 爆仓"
- 交易时间表中的"T+0""T+1"
- 风险对比表中的极端数值
- 术语对照表中的核心术语

**写法**：
```markdown
| 10% | **<mark>10 倍</mark>** | 10% | **<mark>100% 爆仓</mark>** |
```

---

## A5 · 代码块语言标识

**目标**：812处无语言标识的 ` ``` ` 添加 `text` 标识。

**现状**：大量公式、流程图、ASCII图使用无标识代码块，VitePress无法正确高亮。

**规则**：
- 公式/计算/纯文本 → ` ```text `
- Python代码 → ` ```python `（已有，15篇量化篇）
- bash命令 → ` ```bash `（已有，15篇量化篇）
- 不确定时统一用 ` ```text `

**操作步骤**：
1. 搜索所有 ` ``` ` 开头且无语言标识的代码块
2. 根据内容判断语言，添加标识
3. 注意：不要误改已有语言标识的代码块

---

## B3 · SVG图补充

**目标**：22个章节缺少可视化图示，补充关键概念SVG。

**现状**：15个SVG只覆盖6个章节（01/03/05/06/07/12），其余22个章节全靠ASCII图或纯文字。

**优先补充**（按概念重要性排序）：

| 优先级 | 章节 | 建议SVG |
|---|---|---|
| P1 | 27-期权策略进阶篇 | 期权盈亏形态图（牛市价差/铁鹰/跨式） |
| P1 | 22-债券与利率深潜篇 | 收益率曲线形态（正常/倒挂/平坦） |
| P1 | 23-外汇交易实战篇 | 套息交易流程图 |
| P2 | 02-现货篇 | 现货买卖流程 |
| P2 | 04-股票篇 | A股交易规则时间线 |
| P2 | 07-交易系统篇 | 交易系统架构图 |
| P3 | 08-入土篇 | 骗局识别决策树 |
| P3 | 11-交易实战篇 | 网格策略示意图 |
| P3 | 14-理财配置篇 | 资产配置饼图 |

**操作步骤**：
1. 每个SVG先确定要表达的核心概念
2. 参考现有SVG风格（简洁线条+中文标注+暗色友好）
3. 制作SVG放入对应章节 `_assets/` 目录
4. 在md中引用 `![描述](_assets/xxx.svg)`

---

## D1 · 交互式计算器

**目标**：关键公式做成嵌入式Vue计算器组件。

**适合做计算器的公式**：

| 计算器 | 输入 | 输出 |
|---|---|---|
| 杠杆盈亏计算器 | 本金、杠杆倍数、价格涨跌幅 | 盈亏金额、盈亏百分比、是否爆仓 |
| 保证金计算器 | 合约价值、保证金比例 | 开仓保证金、杠杆倍数、强平价位 |
| 期权定价器（Black-Scholes） | 标的价、行权价、到期日、波动率、无风险利率 | Call/Put理论价、Delta/Gamma/Theta/Vega |
| 盈亏比计算器 | 胜率、平均盈利、平均亏损 | 期望值、盈亏比 |

**技术方案**：
- VitePress支持在 `.md` 中直接使用Vue组件
- 组件放在 `docs-site/.vitepress/theme/components/`
- 在md中用 `<LeverageCalc />` 引入
- 需在 `theme/index.js` 中注册全局组件或使用 `enhanceApp`

---

## D2 · 学习路径可视化

**目标**：根README的ASCII学习路线图改为VitePress自定义组件。

**现状**：
```
入门基础 → 现货 → 股票 → 期货 → 加密合约 → 市场专题 → 技术分析 → 交易系统 → 入土避坑
   ↓（进阶）
实战 → 市场生态 → 金融历史 → ...
```

**改进方案**：
- 做成可交互的路线图组件：点击节点跳转对应章节
- 按角色高亮路径（如"股民"路径高亮04→18→19→11）
- 支持暗色/亮色主题

**技术方案**：
- Vue SFC组件，用CSS Grid/Flex布局
- 节点数据从config中读取，与VitePress侧边栏配置同步
- 放在根README.md中替换ASCII图

---

## 执行顺序

```
第1步：A6 示范改造（1篇）→ 本地验证渲染效果
       ↓ 确认样式无误
第2步：B2 死链接排查 → 输出死链接清单 → 修复
       ↓
第3步：C2 风险提示统一 + C4 frontmatter（可并行，都是批量文本替换）
       ↓
第4步：A1 术语高亮 + A2 容器补充（逐篇做，可合并执行）
       ↓
第5步：B1 编号修复 + C1 术语统一 + C3 目录统一（结构类，一次性做）
       ↓
第6步：A3 折叠 + A4 表格高亮 + A5 代码块标识（批量可做）
       ↓
第7步：B3 SVG补充（设计工作，按优先级逐个做）
       ↓
第8步：D1 计算器 + D2 路线图（开发工作，独立推进）
```

---

## 优先级汇总

| 级别 | 编号 | 任务 | 工作量 |
|---|---|---|---|
| P0 | A6 | 示范改造→验证 | 1-2篇 |
| P0 | B2 | 死链接排查 | 254处验证 |
| P0 | C2 | 风险提示格式统一 | 435处 |
| P0 | C4 | frontmatter补充 | 120+篇 |
| P1 | A1 | 术语高亮 | 120+篇逐篇 |
| P1 | A2 | 四色容器补充 | 120+篇逐篇 |
| P1 | B1 | 20章编号修复 | 2个文件+引用 |
| P1 | C1 | 术语统一 | 621处 |
| P1 | C3 | 目录统一 | 63篇 |
| P2 | A3 | 可折叠内容 | 按需 |
| P2 | A4 | 表格关键单元格高亮 | 按需 |
| P2 | A5 | 代码块语言标识 | 812处 |
| P2 | B3 | SVG图补充 | 9+个SVG |
| P2 | D1 | 交互式计算器 | 4个Vue组件 |
| P2 | D2 | 学习路径可视化 | 1个Vue组件 |