# vitepress 1.6.4 附带的 @docsearch/react 3.x 声明 peer react <19，
# 但本项目文档站使用 minisearch 本地搜索、从不加载 docsearch 组件。
# 宽松 peer 处理避免 npm 10（CI Node 22）严格校验 react@18 嵌套树失败；
# 等 vitepress 2 正式版（react 19 peer 或移除 docsearch）后可移除本配置。
legacy-peer-deps=true
